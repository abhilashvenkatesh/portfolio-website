## 1. Nav Component

- [ ] 1.1 Add `<Link href="/">` wrapping `<span>abhilash</span>` to `Nav.tsx` top-left
- [ ] 1.2 Apply Tailwind classes: `font-mono text-sm text-[var(--color-tertiary)] tracking-[0.05em] no-underline`
- [ ] 1.3 Update nav header layout to `justify-between` so logo sits left and links sit right

## 2. Tests

- [ ] 2.1 Write test: logo link renders with text "abhilash" and `href="/"`
- [ ] 2.2 Write test: logo does NOT have `nav-link-active` class/style when on home page

## 3. CI Verification

- [ ] 3.1 Run `npm run typecheck` — passes
- [ ] 3.2 Run `npm run lint` — passes
- [ ] 3.3 Run `npm run build` — passes
